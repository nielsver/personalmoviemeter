import { CapfirstletterPipe } from './capfirstletter.pipe';

describe('CapfirstletterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapfirstletterPipe();
    expect(pipe).toBeTruthy();
  });
});
